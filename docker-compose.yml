# Docker Compose configuration for dmca-light-api
# Generated by The Anvil S+++ Constitutional AGI Genesis Engine
# Version: 0.1.0

version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dmca-light-api-app
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - DEBUG=false
    depends_on:
    volumes:
      - ./app:/app/app
    restart: unless-stopped
    networks:
      - app-network



networks:
  app-network:
    driver: bridge

volumes:
